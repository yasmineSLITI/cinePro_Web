{% extends 'baseBackOffice.html.twig' %}

{% block backContainer %}

    <center><h3> Identifiant du compte : {{ compte.username }} </h3></center>
<section class="content">
        <div class="container-fluid">
            <div class="row">
                <div  class="form-control" style="height: 45rem; margin-top: 5rem;">
                   
                        <div class="card-body">
    <table class="table">
        <tbody>
            <tr>
                <th>Username</th>
                <td>{{ compte.username }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ compte.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ compte.prenom }}</td>
            </tr>
            <tr>
                <th>Mail</th>
                <td>{{ compte.mail }}</td>
            </tr>
            <tr>
                <th>Mdp</th>
                <td>{{ compte.password }}</td>
            </tr>
            <tr>
                <th>Role</th>
                <td>{{ compte.role }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td><img height='75px' src="{{ asset('uploads/'~compte.image) }}"/></td>
            </tr>
        </tbody>
    </table>

    <center><a href="{{ path('app_compte_index') }}"><button type="button" class="btn btn-dark mb-2">Retourner</button></a></center>

    {{ include('compte/_delete_form.html.twig') }}
    </div>
                    
                </div>
            </div>
        </div>

    </section>
     
{% endblock %}
