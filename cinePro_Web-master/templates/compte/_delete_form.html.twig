<form method="post" action="{{ path('app_compte_delete', {'username': compte.username}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ compte.username) }}">
    <button class="btn btn-dark mb-2">Delete</button>
</form>
